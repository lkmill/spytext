				//endBlock = rng.endContainer.closest('[spytext-field] > *'); 
				//var extraction = S.extract();
				//var nodes = extraction.childs();
				//var output = [];
				//var isBlocks;
				//if(nodes[0].nodeType === 1 && nodes[0].isBlock()) {
				//	nodes.each(function() {
				//		var container = wrapper.clone();
				//		while(this.firstChild){
				//			container.append(this.firstChild);
				//		}
				//		if(this === nodes[0]) {
				//			startBlock.append(container);
				//		} else if (this === nodes[nodes.length - 1]) {
				//			endBlock.prepend(container);
				//		} else {
				//			this.prepend(container);
				//			endBlock.before(this);
				//		}
				//	});
				//} else {
				//	nodes.each(function() {
				//		wrapper.append(this);
				//	});
				//	wrapper.tidy();
				//	var node = S.get.rightElement(this.currentField.element);
				//	node.after(wrapper);

				//}
